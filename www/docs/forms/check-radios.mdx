---
title: Check and Radio
description: Render checkbox, radio button, and switch controls with Bootstrap styling and form control support.
---

# Check and Radios

The `Check` component provides a unified interface for rendering checkbox, radio button, and switch controls. It supports controlled and uncontrolled states, disabled state, inline layout, custom values, and Bootstrap-compatible utility props.

## Import

```jsx
import { Form } from "@promethey/bsui";
```

> **Note**
>
> `Check` inherits all utility props supported by the `Prime` component, including layout, spacing, typography, colors, borders, sizing, positioning, and other Bootstrap utility APIs.

## Basic usage

```jsx live
import { Form } from "@promethey/bsui";

export default function Basic() {
  return <Form.Check label="Default checkbox" />;
}
```

## Checkbox

The default `type` is `checkbox`.

```jsx live
import { Form } from "@promethey/bsui";

export default function Checkbox() {
  return (
    <>
      <Form.Check label="Option one" />
      <Form.Check label="Option two" />
      <Form.Check label="Option three" />
    </>
  );
}
```

## Checked

Use `defaultChecked` to define the initial checked state.

```jsx live
import { Form } from "@promethey/bsui";

export default function Checked() {
  return (
    <>
      <Form.Check label="Unchecked checkbox" />
      <Form.Check label="Checked checkbox" defaultChecked />
    </>
  );
}
```

## Radio

Use `type="radio"` for mutually exclusive options.

```jsx live
import { Form } from "@promethey/bsui";

export default function Radio() {
  return (
    <>
      <Form.Check
        type="radio"
        name="options"
        label="First option"
        defaultChecked
      />
      <Form.Check type="radio" name="options" label="Second option" />
      <Form.Check type="radio" name="options" label="Third option" />
    </>
  );
}
```

## Switch

Use `type="switch"` to render a Bootstrap switch control.

```jsx live
import { Form } from "@promethey/bsui";

export default function Switch() {
  return (
    <>
      <Form.Check type="switch" label="Enable notifications" />
      <Form.Check type="switch" label="Dark mode" defaultChecked />
    </>
  );
}
```

## Disabled

Use the `disabled` prop to prevent user interaction.

```jsx live
import { Form } from "@promethey/bsui";

export default function Disabled() {
  return (
    <>
      <Form.Check label="Disabled checkbox" disabled />
      <Form.Check label="Disabled checked checkbox" disabled defaultChecked />
    </>
  );
}
```

## Inline

Use the `inline` prop to display controls horizontally.

```jsx live
import { Form } from "@promethey/bsui";

export default function Inline() {
  return (
    <>
      <Form.Check inline label="First" />
      <Form.Check inline label="Second" />
      <Form.Check inline label="Third" />
    </>
  );
}
```

## Controlled

Use `checked` and `onChange` for controlled components.

```jsx live
import { useState } from "react";
import { Form } from "@promethey/bsui";

export default function Controlled() {
  const [checked, setChecked] = useState(false);

  return (
    <Form.Check
      label={checked ? "Enabled" : "Disabled"}
      checked={checked}
      onChange={(event) => setChecked(event.target.checked)}
    />
  );
}
```

## Custom value

Use the `value` prop to specify the submitted form value.

```jsx live
import { Form } from "@promethey/bsui";

export default function Value() {
  return <Form.Check label="Accept terms" value="accepted" />;
}
```

## Utility props

All utility props provided by `Prime` are supported.

```jsx live
import { Form } from "@promethey/bsui";

export default function Utilities() {
  return (
    <Form.Check label="Styled checkbox" p={3} shadow="sm" rounded={2} mt={3} />
  );
}
```

## API

### Check

| Prop             | Type                                | Default      | Description                              |
| ---------------- | ----------------------------------- | ------------ | ---------------------------------------- |
| `label`          | `string`                            | —            | Text label associated with the control.  |
| `id`             | `string`                            | generated    | Unique identifier for the input element. |
| `name`           | `string`                            | `null`       | Form field name.                         |
| `type`           | `"checkbox" \| "radio" \| "switch"` | `"checkbox"` | Determines control type.                 |
| `defaultChecked` | `boolean`                           | `false`      | Initial checked state.                   |
| `checked`        | `boolean`                           | —            | Controlled checked state.                |
| `disabled`       | `boolean`                           | `false`      | Disables the control.                    |
| `value`          | `string`                            | `null`       | Value submitted with the form.           |
| `onChange`       | `function`                          | `null`       | Called when checked state changes.       |
| `inline`         | `boolean`                           | `false`      | Displays control inline.                 |
| `className`      | `string \| object`                  | `null`       | Additional CSS classes.                  |
| `style`          | `object`                            | `null`       | Inline styles.                           |

---

### Check.Control

| Prop             | Type                                | Default      | Description               |
| ---------------- | ----------------------------------- | ------------ | ------------------------- |
| `id`             | `string`                            | `null`       | Input identifier.         |
| `name`           | `string`                            | `null`       | Input name.               |
| `type`           | `"checkbox" \| "radio" \| "switch"` | `"checkbox"` | Input type.               |
| `defaultChecked` | `boolean`                           | `false`      | Initial checked state.    |
| `checked`        | `boolean`                           | —            | Controlled checked state. |
| `disabled`       | `boolean`                           | `false`      | Disables input.           |
| `value`          | `string`                            | `null`       | Input value.              |
| `onChange`       | `function`                          | `null`       | Change handler.           |

### Check.Label

| Prop        | Type        | Default | Description                  |
| ----------- | ----------- | ------- | ---------------------------- |
| `children`  | `ReactNode` | —       | Label content.               |
| `htmlFor`   | `string`    | `null`  | Associates label with input. |
| `className` | `string`    | `null`  | Additional CSS classes.      |
| `style`     | `object`    | `null`  | Inline styles.               |
