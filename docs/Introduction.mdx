# Bootstrap-UI

<img src="/bsui-logo.png" width="286" style={{ margin: "24px 0 38px 0" }} />

### Modern Bootstrap component system for React applications.

Bootstrap-UI combines clean Bootstrap architecture with modern React
patterns, full IntelliSense support and production-grade developer
experience — without TypeScript overhead.

```text
React • Bootstrap 5 • Pure JavaScript • JSDoc Types • Storybook
```

<br />

### Why Bootstrap-UI

- No TypeScript complexity
- Full autocomplete and type checking via JSDoc
- Documentation directly inside Storybook
- Native Bootstrap ecosystem compatibility
- Fast integration into existing projects
- Support for latest Bootstrap releases immediately after launch

<br />

# Developer Experience

### Pure JavaScript

Bootstrap-UI is built entirely with modern JavaScript while preserving
strong editor tooling and scalable APIs.

```jsx
import { Button } from "bootstrap-ui";

export function Example() {
  return (
    <Button theme="primary" size="lg">
      Create project
    </Button>
  );
}
```

### Full IntelliSense without TypeScript

```js
/**
 * @typedef {object} ButtonOwnProps
 * @property {"button"|"input"|"a"} [as] - HTML element type used for rendering.
 * @property {Object} [style] - Inline styles applied to the root.
 * @property {React.ReactNode} [children] - Content rendered inside the component.
 * @property {Object|string} [className] - Additional classes applied to the root element.
 */
```

- autocomplete
- prop validation
- hover documentation
- IDE hints (VSCode)
- typed APIs

without `.ts` files or TypeScript configuration.

<br />

# Bootstrap Integration

Bootstrap-UI follows the latest Bootstrap architecture and styling
patterns while extending them with reusable React abstractions.

### Advantages

- Bootstrap 5 compatibility
- predictable class behavior
- utility-first integration
- minimal styling conflicts
- immediate support for new Bootstrap releases

<br />

# Storybook-first Documentation

Every component includes:

- live examples
- controls
- prop documentation
- usage guidelines
- JSDoc integration
- accessibility notes

<br />

# Quick Example

```jsx
import { Button, Card } from "bootstrap-ui";

export function Example() {
  return (
    <Card style={{ width: "18rem" }}>
      <Card.Body>
        <Card.Title>Card title</Card.Title>
        <Card.Text>
          Some quick example text to build on the card title and make up the
          bulk of the card&apos;s content.
        </Card.Text>
        <Button>Go somewhere</Button>
      </Card.Body>
    </Card>
  );
}
```

# Installation

```bash
npm i bsui
```

# Tooling

Bootstrap-UI is optimized for modern frontend workflows.

```text
Vite • Storybook • ESLint • JSDoc • Bootstrap 5
```

<br />

# Accessibility

Components include built-in support for:

- keyboard navigation
- focus management
- semantic HTML
- ARIA attributes
- accessible interaction patterns

<br />

---

### Bootstrap-UI

Enterprise-ready React components powered by Bootstrap.
