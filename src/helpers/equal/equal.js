/**
 * Function for strictly comparing the first value with the second
 *
 * @param {*} value1
 * @param {*} value2
 * @returns {boolean} result
 */
export function equal(value1, value2) {
  return value1 === value2;
}
